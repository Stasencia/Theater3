<Window x:Class="WpfApp1.Afisha"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="Afisha" Height="530" Width="800">
    <Grid Margin="0,30,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="31*"/>
            <RowDefinition Height="31*"/>
            <RowDefinition Height="183*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0"  Background="Tomato" />
        <Label Foreground="White" FontFamily="Century" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="338,8" Height="46" Width="116">Афиша</Label>
        <StackPanel x:Name="month_panel" Grid.Row="1" Orientation="Horizontal"></StackPanel>
        <ScrollViewer Grid.Row="2">
            <StackPanel x:Name="performancelist_panel"></StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
